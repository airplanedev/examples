name: Hello World
description: Greets you by name!
slug: hello_world

builder: deno
builderConfig:
  entrypoint: main.ts

arguments: ["--name", "{{.name}}"]
parameters:
  - name: Name
    slug: name
    type: string
    default: World
