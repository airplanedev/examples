name: Hello World
slug: hello_world
description: Greets you by name!

builder: go
builderConfig:
  entrypoint: main.go

arguments: ["--name", "{{.name}}"]
parameters:
  - name: Name
    slug: name
    type: string
    default: World
