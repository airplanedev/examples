name: Hello World
description: Greets you by name!
slug: hello_world

builder: go
builderConfig:
  entrypoint: main.go

arguments: ["--name", "{{.name}}"]
parameters:
  - name: Name
    slug: name
    type: string
    default: World
