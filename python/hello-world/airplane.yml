name: Hello World
slug: hello_world
description: Greets you by name!

builder: python
builderConfig:
  entrypoint: main.py

arguments: ["--name", "{{.name}}"]
parameters:
  - name: Name
    slug: name
    type: string
    default: World
