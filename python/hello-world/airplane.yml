name: Hello World
description: Greets you by name!
slug: hello_world

builder: python
builderConfig:
  entrypoint: main.py

arguments: ["--name", "{{.name}}"]
parameters:
  - name: Name
    slug: name
    type: string
    default: World
